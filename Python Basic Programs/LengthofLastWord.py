class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        i = ""
        for i in str:
            newStr = str[-1:]
            length = len(newStr)
        return length
    
Solution().lengthOfLastWord("Hello World")